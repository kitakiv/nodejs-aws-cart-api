services:
  api:
    image: kitakiv/eb-image:latest
    ports:
      - "80:4000"
    environment:
      - NODE_ENV=production
      - APP_PORT=${APP_PORT}
      - AUTH_USERNAME=${AUTH_USERNAME}
      - AUTH_PASSWORD=${AUTH_PASSWORD}
      - APP_URL=${APP_URL}
    restart: always